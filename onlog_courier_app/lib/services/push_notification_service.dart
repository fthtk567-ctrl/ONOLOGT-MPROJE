import 'package:flutter/material.dart';

class PushnotificationService extends StatelessWidget {
  const PushnotificationService({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('Çevriliyor...')));
}
